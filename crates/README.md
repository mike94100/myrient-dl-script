# crates/

This directory contains Rust helper crates and CLI tools that are part of the workspace.

Build all workspace crates with:

```bash
cargo build --workspace
```

Individual crates can be built with:

```bash
cargo build -p myrient-gen
```
